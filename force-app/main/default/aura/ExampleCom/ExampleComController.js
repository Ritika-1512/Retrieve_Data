({
	doInit : function(component, event, helper) {
		/*var abc= component.get("v.Student");
        var xyz = component.set("v.Student",abc);
        
        
        var action= component.get("c.method");
        action.setParams({
            "list": "abc",
        })
        
        $A.enqueueAction(action);
        action.setCallback(this,funtion(response){
                           var state = response.getState();
        alert(state);
        
        
        if(state === 'suu'){
            var result = component.getreturnValue();
            response.set("v.Student", result)
        }
                           })*/
	}
})